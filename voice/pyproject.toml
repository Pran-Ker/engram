[project]
name = "voice"
version = "0.1.0"
description = "Fine-tune LFM2.5-Audio-1.5B on Prannay's voice (record locally, train on Modal)"
requires-python = ">=3.12,<3.13"
dependencies = [
    "sounddevice>=0.5",
    "soundfile>=0.13",
    "numpy>=2.0",
    "modal>=1.4",
    "mlx-whisper>=0.4",
]

[tool.uv]
package = false
